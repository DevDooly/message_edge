<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Overlay permission for floating Edge handle & panel -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Foreground Service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- Post notifications for service status -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Haptic feedback on edge gesture -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Auto start after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Request ignore battery optimization -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Internet for checking & downloading GitHub Releases update -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".NotificationEdgeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotificationEdge"
        tools:targetApi="34">
        
        <!-- 런처 메인 엔트리 포인트 (설정 및 대시보드 화면으로 이동) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NotificationEdge"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 설정 화면 전용 액티비티 -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.NotificationEdge"
            android:label="@string/app_name"
            android:noHistory="false"
            android:excludeFromRecents="false" />

        <!-- Quick Settings 빠른 설정 상단바 타일 서비스 (무액티비티 엣지 토글) -->
        <service
            android:name=".service.EdgeTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Good Lock (One Hand Operation +) / 루틴 / Broadcast Intent용 무액티비티 리시버 (미디어 재생 완벽 유지) -->
        <receiver
            android:name=".service.OpenPanelReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.devdooly.notificationedge.OPEN_PANEL" />
                <action android:name="com.devdooly.notificationedge.CLOSE_PANEL" />
                <action android:name="com.devdooly.notificationedge.TOGGLE_PANEL" />
                <action android:name="com.devdooly.notificationedge.ACTION_OPEN_PANEL" />
                <action android:name="com.devdooly.notificationedge.ACTION_CLOSE_PANEL" />
                <action android:name="com.devdooly.notificationedge.ACTION_TOGGLE_PANEL" />
            </intent-filter>
        </receiver>

        <!-- Notification Listener Service -->
        <service
            android:name=".service.NotificationListener"
            android:exported="true"
            android:label="@string/notification_listener_service_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Edge Overlay Floating Service -->
        <service
            android:name=".service.EdgeOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Overlay Edge Panel for Notifications" />
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for APK In-App Update Installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
